import axios from 'axios';

/*
  Example:

  export function fetchCurrentOrder() {
    return axios.get('/orders/current');
  }

  export function createLineItem({ orderNumber, variantId, quantity = 1 }) {
    return axios.post(`/orders/${orderNumber}/line_items`, {
      variantId,
      quantity
    });
  }
*/
