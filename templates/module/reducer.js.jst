import { Map } from 'immutable';

const initialState = Map();

function <%= lowerModuleName %>Reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default <%= lowerModuleName %>Reducer;
