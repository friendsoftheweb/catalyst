{
  "author": "Dan Martens",
  "license": "MIT",
  "engines": {
    "node": ">= 10"
  },
  "scripts": {
    "test": "lerna run test",
    "types:check": "lerna run types:check",
    "prettier:check": "lerna run prettier:check",
    "lint": "lerna run lint",
    "clean": "scripts/clean",
    "postinstall": "scripts/postinstall",
    "lerna:publish": "lerna publish",
    "lerna:publish:next": "lerna publish --dist-tag next"
  },
  "devDependencies": {
    "lerna": "^3.14.1"
  }
}
